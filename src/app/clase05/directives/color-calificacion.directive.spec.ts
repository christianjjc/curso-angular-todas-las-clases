import { ColorCalificacionDirective } from './color-calificacion.directive';

describe('ColorCalificacionDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCalificacionDirective();
    expect(directive).toBeTruthy();
  });
});
